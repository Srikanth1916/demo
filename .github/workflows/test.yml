name: 'testing'

on:
  workflow_dispatch

jobs:
  testing:
    name: 'testing'
    runs-on: ubuntu-latest
    defaults:
      run:
        shell: bash

    steps:
    - name: Repo Checkout
      uses: actions/checkout@v2
  
         
    - name: checking the release tag number
      run: |
       echo $GITHUB_REF
       echo ${{ github.ref }}
